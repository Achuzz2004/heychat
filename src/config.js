export const API_BASE = window.__API_URL__ || "http://localhost:8000";
export const WS_BASE = window.__WS_URL__ || "ws://localhost:8000";

export const SEO = {
  title: "Chatter — Secure Real‑Time Chat",
  description:
    "End-to-end encrypted real-time chat. One-to-one messages, receipts, presence, media.",
};
